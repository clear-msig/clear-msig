[package]
name = "clear-msig-signing"
version = "0.1.0"
edition = "2021"
license = "BSD-3-Clause-Clear"

[features]
default = ["std"]
std = []

[dependencies]
sha2 = { version = "0.10", default-features = false }
